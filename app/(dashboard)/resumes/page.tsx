import { VersionFolderView } from "@/components/resume/VersionFolderView";

export const metadata = { title: "履歷版本 — AI Hunter" };

export default function ResumesPage() {
  return <VersionFolderView />;
}
